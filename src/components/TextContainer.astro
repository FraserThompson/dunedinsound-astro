---
import { textContainer } from './TextContainer.css'

interface Props {
	featureMode?: boolean
}

const { featureMode } = Astro.props
---

<div class={textContainer[featureMode ? 'feature' : 'normal']}>
	<slot />
</div>
