---
import { cornerLabelWrapper } from './CornerLabel.css'

/**
 * Label in the corner of the latest gig.
 */

interface Props {
	position?: 'left' | 'right'
}

const { position } = Astro.props
---

<div class={cornerLabelWrapper[position || 'left']}>
	<slot />
</div>
